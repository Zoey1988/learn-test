import { mutations } from '.'

test('"login" changes "state.isLoggedIn" to true', () => {
    // 🧭🧭 time: 5 minutes
    // 💰 1) create state
    // 💰 2) call mutation
    // 💰 3) check if state changes are true
})

// test logout action
// 🧭🧭 time: 5 minutes